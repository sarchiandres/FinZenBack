spring:
  datasource:
    url: jdbc:mysql://localhost:3306/FinZen?useSSL=false&serverTimezone=UTC
    username: root
    password: sarchi2006
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect # Usa el dialecto estándar
        format_sql: true
        jdbc:
          batch_size: 20
    open-in-view: false # Recomendado para aplicaciones REST

# Configuración adicional recomendada
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    org.springframework.web: debug